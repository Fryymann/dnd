# Campaigns

Use one folder per campaign for campaign-owned material such as character ties, session work, setting notes, prep, handouts, and campaign-specific rules.

Keep broadly reusable material in `../resources/`. If a campaign grows into an independent project with its own tooling, collaborators, or release history, it can later be moved to its own repository and linked here as a Git submodule.
